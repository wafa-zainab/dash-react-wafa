import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // your global styles (tailwind or other)

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
